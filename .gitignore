# Scraper tooling (local only)
ba/

# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.aws-sam/
*.tsbuildinfo

# Env / secrets (never commit)
.env
.env.local
.env.*.local
!.env.example

# Claude Code per-user settings
.claude/settings.local.json

# Editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage/
*.lcov
.vitest-cache/

# Generated knowledge graph (regenerate with `graphify update .`)
graphify-out/

# OAB 2ª-fase import drafts (review artifacts; data lives in the DB)
scripts/out/

# Xenomoon autonomous-agent local state (machine-managed, churns constantly)
.xenomoon/

# Applied-migrations marker (per-machine local state; written at db:migrate, read
# by the migrate push-guard). Not repo content — never commit.
drizzle/meta/_applied.json

# Eval harness outputs (promptfoo cache + results)
evals/.promptfoo/
evals/results/latest.json
evals/node_modules/

# Xenomoon Forge generated/working files — not repo content
.claude/projects/
